---
# ── COPY THIS FILE to src/content/projects/<your-slug>/index.mdx ──
# The folder name becomes the URL: /projects/<your-slug>
# This _template folder never renders on the site.
# Required fields first — only these block a publish. Optional ones never do.

title: My Project Name

# exactly one of: live | dead | paused
status: live

# YYYY-MM-DD — the feed sorts by this, newest first
date: 2026-01-01

# at least one of each; lowercase, hyphenated. Reuse existing tags where
# possible — a tag used by only one project won't connect in the graph.
tags: [automation]
stack: [python]

# One sentence. The result, with a number if you have one.
# For dead projects: what killed it.
outcome: Cut processing time from X to Y for a Z-person team.

# ── OPTIONAL — delete any line you don't need ──
# org_context: Mid-size logistics company, finance ops
# links:
#   youtube: "https://youtu.be/..."
#   linkedin: "https://www.linkedin.com/posts/..."
#   live: "https://..."
#   repo: "https://github.com/..."
# hero_asset: ./assets/hero.png   # card thumbnail; put the file in this folder's assets/
---

Free-form MDX body — write the case study here. Plain markdown works.

Put images in an `assets/` folder next to this file and reference them
relatively, like this (kept as a code example — a real image line whose
file doesn't exist yet will fail the build):

```md
![what the diagram shows](./assets/pipeline.png)
```
